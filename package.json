{
  "name": "no.almli.thermostat",
  "version": "1.1.15",
  "description": "Virtual thermostat",
  "main": "app.js",
  "scripts": {
    "pretest": "eslint --ignore-path .gitignore app.js drivers lib test",
    "test": "./node_modules/.bin/mocha --reporter spec",
    "posttest": "athom app validate --level publish"
  },
  "author": "Bjørnar Almli",
  "license": "ISC",
  "dependencies": {
    "athom-api": "^2.1.215"
  },
  "devDependencies": {
    "eslint": "^5.12.1",
    "chai": "^4.2.0",
    "mocha": "^5.2.0"
  }
}
